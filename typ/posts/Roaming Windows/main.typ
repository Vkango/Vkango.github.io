#import "@preview/typst-apollo:0.1.0": pages
#import "@preview/shiroa:0.2.3": *
#import pages: *

#show: project.with(
  title: "漫游 Windows 图标渲染的三十年变革",
  authors: (
    (
      name: "Vkango",
      email: "hivkan@outlook.com",
    ),
  ),
)

#set par(justify: true)
#set heading(numbering: "1.1.")
#set text(font: ("New Computer Modern", "Source Han Serif"))
#show math.equation: set text(font: ("New Computer Modern Math", "Source Han Serif"))

#import "@preview/ctheorems:1.1.3": *
#show: thmrules.with(qed-symbol: $square$)

#let definition = thmbox(
  "definition",
  "Definition",
  inset: (top: 10pt, bottom: 10pt, left: 10pt, right: 10pt),
  radius: 0pt,
  stroke: (bottom: 1pt, top: 1pt),
)

#let thinking = thmbox(
  "thinking",
  "Thinking",
  inset: (top: 10pt, bottom: 10pt, left: 10pt, right: 10pt),
  radius: 0pt,
  stroke: (bottom: 1pt, top: 1pt),
)

_Welcome home. Consider this an odyssey of the mind._

这是一个头脑风暴文章, 目的是拓宽视野, 将多个领域知识穿插理解, 可能并不严谨.

#v(50pt)

一个图标真正出现在屏幕上, 至少要跨过四道门槛:

- 它最初是像素点阵, 还是连续曲线.

- 每个像素该显示什么颜色, 由 CPU 算, 还是由 GPU 算.
- 中间数据存在哪里, 显存和带宽够不够.
- 最后的 RGB 数值, 怎样被显示器扫描并变成人眼看到的亮度.

#v(40pt)

= 屏幕与图形

== 屏幕本质上是离散网格

从数学上说, 屏幕可以建模为一个二维离散格点集合:

$
  Omega = { (i, j) | 0 <= i < W, 0 <= j < H }.
$

这里的 $W$ 和 $H$ 分别是横向和纵向的像素数. 对于一张 32 位 ARGB 图像, 我们也可以把帧缓冲区看成一个映射:

$
  I: Omega -> {0, ..., 255}^4.
$

也就是说, 显存里的图像, 本质上就是一个二维离散数组; 每个位置都存着四个 8-bit 通道: Alpha、Red、Green、Blue.

== 连续图形为什么会变成锯齿

设计师脑中的圆角图标, 本质上是平面中的一个连续区域 $S subset RR^2$. 但显示器只能接受离散像素网格, 所以系统必须把连续边界投到离散方格上. 最粗糙的办法是只看像素中心点是否落在图形内部: 在里面就着色, 不在里面就留给背景.

问题也正出在这里. 连续曲线斜着切过方格时, 有限采样率会丢掉边界的高频信息, 于是视觉上就出现锯齿.

== 抗锯齿技术

如果把第 $(i, j)$ 个像素对应的小方格记作 $P_(i, j)$, 并把图形与该像素方格的重叠面积记作 $A_(i, j)$, 那么一个更好的做法不是只问"像素中心在不在里面", 而是估计这个小方格到底被图形覆盖了多少面积:

$
  alpha_(i, j) = A_(i, j) / abs(P_(i, j)).
$

于是边缘像素的颜色就可以写成

$
  C_"out" = alpha C_"src" + (1 - alpha) C_"dst".
$

这就是抗锯齿最重要的几何直觉. 边缘像素之所以半透明, 不是因为它"半存在", 而是因为曲线只覆盖了这个像素方格的一部分面积.

== CPU、GPU 与显示器是一条链路

很多文章会把这件事说成"CPU 负责计算, GPU 负责画图". 这个说法不算错, 但还是太粗了. 更完整的链路其实是:

- 应用或系统进程决定显示哪个图标、放在哪、缩放多少.

- CPU 组织命令、准备资源、调用图形 API.
- GPU 执行光栅化、纹理采样、混合与着色.
- 显存存放纹理、缓冲区和当前帧.
- 显示控制器按时序从帧缓冲区扫描输出.
- LCD 或 CRT 再把这些离散数值转成真实亮度.

也就是说, 屏幕既不是 CPU 直接"怼"出来的, 也不是 GPU 单独"变"出来的. 它是一整条数据通路:

$
  & "UI Logic" \
  & -> "API Commands" \
  & -> "GPU Rendering" \
  & -> "Frame Buffer" \
  & -> "Scan-out" \
  & -> "Display"
$

= 硬边缘时代

== 显存、调色板与小尺寸图标

在 Windows 95、98、2000 那个年代, 图形系统首先考虑的不是漂亮, 而是省内存、省带宽. 显存只有几百 KB 到几 MB 时, 分辨率、色深和窗口内容很快就会把资源吃满, 所以小尺寸图标、4-bit 或 8-bit 调色板模式都非常常见.

在 8-bit 模式里, 像素里存的甚至不是真实 RGB, 而只是一个调色板索引:

$
  I(i, j) in {0, ..., 255}.
$

真实颜色则由查色表给出:

$
  "RGB"(i, j) = "Palette"[I(i, j)].
$

这是一种非常典型的工程折中. 它牺牲颜色自由度, 换来更低的存储成本和更低的传输开销.

== GDI 和 BitBlt 的世界观

Windows 早期的图形系统核心是 GDI. 它的设计目标是给应用程序一套统一的设备无关接口, 让程序员不必直接操作显卡. GDI 的世界观非常鲜明: *一切图形最终都要落到位图上*.

这也是 BitBlt 如此重要的原因. 它本质上就是一次二维数组搬运:

$
  D[x:x+w, y:y+h] <- S[u:u+w, v:v+h].
$

如果叠加逻辑光栅操作, 可以直接转换为"按位复制、与、或、异或"的位操作. 这是一个很典型的离散系统, 它更关心位模式如何变换, 而不是几何边界如何平滑.

== 1-bit 透明度为何天生粗糙

早期图标文件通常由颜色位图和透明掩码共同组成. 这里的透明度不是连续变量, 而是一个布尔值: 要么透明, 要么不透明. 常见流程是先用 AND mask 在背景上挖空, 再用 XOR 或其他 ROP 组合把图标颜色写回去.

这意味着边缘不可能出现真正的灰阶过渡. 一个圆被放到离散网格上时, 只能以台阶状近似它的轮廓. 于是设计师不得不大量依赖两种技巧:

- 直接手工摆像素, 也就是典型的 pixel art.

- 用深色描边增强局部对比度, 避免图标在复杂背景里"散掉".

从今天回看, 这些图标之所以风格如此鲜明, 很大程度上就是因为渲染管线只允许它们这样长.

== CRT 技术

一个很容易被忽略的因素是, 早期显示器大多还是 CRT. CRT 的光斑扩散、模拟扫描和边缘轻微发虚, 会天然带来一点低通滤波效果, 于是锯齿没那么刺眼.

后来 LCD 像素边界更硬、更整齐, 很多旧图标反而一下子显得更硌眼了.

= 柔和边缘时代

== Alpha 通道

到了 Windows XP, 透明度终于从 1-bit 变成了 8-bit. 像素变成了 $(A, R, G, B)$, 其中

$
  A in {0, ..., 255}.
$

透明度从布尔值变成 256 级离散标量之后, 图标终于可以和背景做连续混色, 不用做逻辑替换了.

== Alpha Blending 把图像合成变成实数运算

最经典的 Alpha 混合公式是:

$
  C_"out" = alpha C_"src" + (1 - alpha) C_"dst".
$

它的含义非常直白: 输出颜色一定落在前景色和背景色之间. 边缘像素如果只覆盖了 30% 的面积, 那么它看起来就像 30% 的图标颜色和 70% 的背景颜色混在一起.

更进一步地, 现代图形系统常常使用*预乘 Alpha*. 也就是先把源颜色乘上透明度, 再进入后续插值与混合流程. 这样做的好处是边缘不会那么容易出现脏色或色边, 也更适合 GPU 管线中的重采样与插值.

#thinking[
  抗锯齿真正依赖的不是"文件里有一个 Alpha 通道", 而是渲染器愿意为边缘像素估计覆盖率, 并把这个覆盖率编码进 Alpha.
]

== 多分辨率 ICO 与缩放问题

XP 到 Vista 这一段还有一个很重要的变化: 图标不再只是固定大小的小位图, 而更像一个多分辨率资源包. 一个 .ico 文件里可以同时放入 16x16、32x32、48x48、64x64, 甚至 256x256 的多个版本. Vista 之后, 256x256 图标还可以直接使用 PNG 压缩.

缩放位图发糊, 本质上也是重采样问题. 最近邻会块, 双线性会软, 双三次会更平滑但也更贵. 无论哪种方法, 它们都只能在已有样本之间插值, 不可能凭空创造真正的边缘信息.

== Vista / 7 的 DWM 改变了桌面

Windows Vista 引入 DWM 之后, 桌面图形架构真正拐了一个弯. 在这之前, 窗口内容更接近直接画到屏幕或共享桌面表面; 在这之后, 每个窗口先渲染到自己的离屏表面, 再由系统统一合成.

可以把它抽象地写成:

$
  "Desktop" = sum_k "Compose"(W_k, T_k, alpha_k).
$

这里的 $W_k$ 是第 $k$ 个窗口内容, $T_k$ 是几何变换, $alpha_k$ 是透明度或其他合成参数. 把窗口当成纹理贴到矩形上, 再交给 GPU 做采样和混合, 这件事对于 3D 图形管线来说几乎是本能.

所以 Vista 和 Windows 7 时代那些看起来像"桌面特效"的东西, 本质上是桌面第一次被系统性地看成一个可合成场景. 图标也因此进入了更统一的混合和缩放体系.

= 高 DPI 与矢量时代

== 从 96 DPI 到逻辑像素

传统 Windows UI 长期围绕 96 DPI 设计. 这个假设在低分辨率屏幕上问题不大, 但一旦笔记本开始塞进 2K、4K 面板, 旧坐标系立刻就失灵了: 图标太小、位图放大变糊、文字 hinting 失真、细线条开始抖动.

于是系统开始明确区分逻辑像素和物理像素. 最核心的关系式可以写成:

$
  "Physical Pixels" = "DIPs" times "DPI" / 96.
$

这就是 DPI 缩放的根本公式. 图标之所以必须升级, 不是因为"屏幕更细了"这么简单, 而是因为过去直接围绕物理像素作图的办法不再可靠.

== 位图记录结果, 矢量记录规则

位图的本质是"每个采样点都已经被定死"; 矢量图的本质则是"我只描述连续几何形状, 最终像素结果可以到显示时再算". 从这个角度看, 两者最大的区别其实只有一句话:

- 位图记录的是结果.
- 矢量记录的是规则.

当分辨率变化时, 位图只能重采样; 矢量则可以重新光栅化. 这就是所谓分辨率无关的真正含义.

== Direct2D 把 2D 渲染从像素搬运推向几何处理

Direct2D 的历史地位在于, 它代表 Windows 2D 渲染从 GDI 系向 DirectX 系迁移. 它处理的不再只是位图搬运, 而是线段、圆弧、Bezier 曲线、路径几何、渐变、几何变换与填充规则.

也就是说, 绘制对象不再只是 pixel array, 而是 geometric primitives. UI 资产也不必一开始就固化成某个固定尺寸的结果图, 而可以在运行时根据目标 DPI 重新生成更合适的像素输出.

== 贝塞尔曲线、三角形与解析覆盖

很多现代图标边缘都由贝塞尔曲线描述. 以二次贝塞尔曲线为例:

$
  B(t) = (1 - t)^2 P_0 + 2(1 - t) t P_1 + t^2 P_2, quad t in [0, 1].
$

三次贝塞尔曲线则写成:

$
  B(t) = (1 - t)^3 P_0 + 3(1 - t)^2 t P_1 + 3(1 - t) t^2 P_2 + t^3 P_3.
$

它们允许设计师只用少量控制点, 就定义出光滑边界. 但 GPU 并不直接"理解曲线". 进入渲染流水线之后, 常见路径通常还是:

- 解析路径与控制点.
- 把曲线细分成足够密的几何片段.
- 生成三角形网格.
- 光栅化、着色、混合输出.

这就是为什么现代图标看起来像数学曲线, 底层却常常仍然是大量细小三角形的近似结果.

当然, 现代 2D 渲染也不只靠暴力细分. 解析覆盖估计、signed distance field、analytic anti-aliasing、Loop-Blinn 一类经典曲线渲染思路, 也都在不同场景里扮演过角色. 更稳妥的说法是: *现代矢量渲染既可能走几何细分路径, 也可能结合像素阶段的解析覆盖估计来提升边缘质量和效率*.

== DirectWrite 与图标字体

到了 Windows 10 和 Windows 11, 一个很有意思的现象出现了: 很多系统里的"图标"其实根本不是图片, 而是字形. 比如 Segoe MDL2 Assets 和 Segoe Fluent Icons, 本质上都是字体文件里的轮廓字形.

对系统来说, 这条链路更像是:

$
  "Code Point" -> "Glyph Outline" -> "Rasterization".
$

这时候真正挑大梁的是 DirectWrite. 它负责布局、轮廓解析、hinting、抗锯齿以及必要时的子像素处理. 也就是说, 很多图标已经被并入了文本渲染体系.

= 从 .ico 到多资产并存

== 为什么 .ico 还没有消失

今天的 Windows 并不是"彻底告别位图", 而是进入了多种图标技术并存的阶段. .ico 仍然非常重要, 因为 Shell、文件关联、EXE 资源、快捷方式以及大量经典 Win32 程序都还依赖它.

它最擅长的场景依旧是:

- 桌面图标和应用图标.
- 文件类型图标.
- 多尺寸资源打包与兼容分发.

所以 .ico 不是过时了, 而是不再独占一切.

== 为什么系统 UI 更偏爱 glyph 与路径

现代系统界面更在意统一线宽、任意缩放、主题着色、动画过渡和与文本系统共享布局. 只要图标本质上还是一条路径, 或者一个字形轮廓, 系统就可以在不同 DPI 下重绘它, 可以按主题动态换色, 也可以和文字共享 baseline、抗锯齿与排版体系.

对 UI 框架来说, 这比维护海量位图资源优雅得多.

== SVG 的意义不只是"可缩放"

SVG 真正重要的地方, 不只是它可以无损放大, 而是它把图形写成了可解析、可变换、可组合的结构化语言. path、fill、stroke、transform、gradient、clip 这些元素, 等于把图标从"一个二进制图片资源"变成了一段图形描述程序.

从工程角度看, SVG 更像是一种声明式图形 DSL. 它让图标更容易被主题化、复用、检查和自动处理.

= 一条更底层的主线

如果把这三十年压缩成一条技术主线, Windows 图标渲染其实完成了三次抽象升级:

- 从逻辑位运算走向连续混色. 早期主要是 AND、XOR 和布尔掩码, 后来变成线性颜色合成.
- 从单一分辨率位图走向多尺度资产和矢量描述. 过去图标更像固定结果, 现在更像一套生成规则.
- 从 CPU 主导的逐像素处理走向 GPU 主导的并行图形流水线. 图标不再只是内存里的小图片, 而是整个渲染管线中的一个节点.

#thinking[
  位图搬运、Alpha 混合、桌面合成、路径几何、字形轮廓、高 DPI 重光栅化, 看起来像很多分散的技术名词; 但把它们串起来之后, 会发现它们都在回答同一个问题: 怎样让离散机器更忠实地逼近连续形状.
]

#figure(
  table(
    columns: (0.95fr, 1.4fr, 1.45fr, 1.7fr),
    inset: 6pt,
    [*时代*], [*图标数据形态*], [*主要 API / 路径*], [*核心问题与解法*],

    [Win95 / 98 / 2000],
    [小尺寸位图 + 掩码],
    [GDI、BitBlt、CPU 为主],
    [透明度只有 1-bit, 边缘粗糙, 依赖 pixel art 和描边处理],

    [XP], [32 位 ARGB 位图], [GDI+、Shell 图标渲染], [Alpha 混合进入主流, 边缘开始平滑, 但缩放仍受限于位图采样],

    [Vista / 7],
    [多分辨率 ICO、PNG 大图标],
    [DWM + Direct3D 合成],
    [桌面被统一看成可合成场景, 窗口与图标进入更一致的 GPU 混合链路],

    [Win8 / 10 / 11],
    [位图、路径、glyph、SVG 并存],
    [Direct2D、DirectWrite、WinUI],
    [高 DPI 成为常态, 系统更依赖重光栅化、路径渲染和图标字体],
  ),
  caption: [Windows 图标渲染三十年的技术主线],
)

= 结语

如果一定要用一句话概括 Windows 图标三十年的变化, 那就是: *图标渲染的历史, 本质上是计算机越来越精确地逼近连续几何世界的历史*.

在 Windows 95 时代, 这种逼近方式是小尺寸位图、黑白掩码和逻辑位运算; 到了 XP、Vista、Windows 7, 它升级成了 32 位颜色、Alpha 混合与 GPU 参与的桌面合成; 而在 Windows 10、11, 它进一步变成路径几何、Bezier 曲线、字形轮廓以及面向高 DPI 的实时重光栅化.

所以图标之所以越来越圆润, 不只是设计师审美变了. 它背后其实是整个 Windows 图形栈完成了一次持续三十年的系统迁移: 从位图搬运到几何渲染, 从 CPU 驱动到 GPU 合成, 从单一分辨率走到高 DPI 自适应.
